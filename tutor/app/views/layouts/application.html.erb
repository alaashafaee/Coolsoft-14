<!--
# [View Layout - Story 1.2]
# General layout
# Author: Ahmed Elassuty
-->
<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Valorous.io</title>
		<%= stylesheet_link_tag "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
		<link rel="stylesheet" href="../assets/alertify-themes/alertify.default.css" id="toggleCSS" />
		<%= csrf_meta_tags %>
		<meta name="viewport" content="width=device-width"/>
	</head>
	<body style="background: #EEEFE9;">
		<div class="navbar navbar-default navbar-fixed-top">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed"
						data-toggle="collapse" data-target=".navbar-responsive-collapse">
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button>
				<a href="../" class="navbar-brand">CoolSoft - Java Tutor</a>
			</div>
			<div class="navbar-collapse navbar-responsive-collapse collapse">
				<% if signed_in? %>
					<%= render "layouts/search_form"%>
				<% end %>
				<ul class="nav navbar-nav navbar-right">
					<% if signed_in? %>
						<li style="padding-right:10px">
							<%= render "layouts/log_out_form" %>
						</li>
					<% else %>
						<div class="navbar-collapse collapse" id="navbar-main">
							<ul class="nav navbar-nav navbar-right">
								<li class="dropdown">
									<a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes">Sign in&nbsp&nbsp
										<span class="caret"></span>
									</a>
									<ul class="dropdown-menu" aria-labelledby="themes">
										<li>
											<a href="../lecturers/sign_in">Lecturer</a>
										</li>
										<li>
											<a href="../teaching_assistants/sign_in">Teaching Assistant</a>
										</li>
										<li>
											<a href="../students/sign_in">Student</a>
										</li>
									</ul>
								</li>
							</ul>
						</div>
					<% end %>
				</ul>
			</div>
		</div>
		<% if signed_in? %>
			<div class="container cont">
				<div class="col-md-10 left_side">
					<%= yield %>
				</div>
				<div class="col-xs-hidden col-md-2 right_side">
						<%= render "layouts/right_side_bar" %>

				</div>
			</div>
		<% else %>
			<div class="row" style="background: #eeefe9;">
					<%= yield %>
			</div> 
		<% end %>
		</br>
		<div align="center" class="navbar navbar-default
			navbar-static-bottom footer" style="min-width:1000px; border-radius: 0px;">
			<%= render "layouts/footer" %>
		</div>
	</body>
</html>
